import { FiltroDatosPipe } from './filtro-datos.pipe';

describe('FiltroDatosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDatosPipe();
    expect(pipe).toBeTruthy();
  });
});
